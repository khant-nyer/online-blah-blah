$(document).ready(function() {
	$('.your-class').slick({
		infinite: true,
		slidesToShow: 4,
		slidesToScroll: 4,
		nextArrow: $('#next'),
		prevArrow: $('#prev'),
		});
	$('.your-class2').slick({
		infinite: true,
		slidesToShow: 4,
		slidesToScroll: 4,
		nextArrow: $('#next2'),
		prevArrow: $('#prev2'),
		});
	
	
	
});